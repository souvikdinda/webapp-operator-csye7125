---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: httpchecks.csye7125-fall2023-group07.operator.souvikdinda.me
spec:
  group: csye7125-fall2023-group07.operator.souvikdinda.me
  names:
    kind: HttpCheck
    listKind: HttpCheckList
    plural: httpchecks
    singular: httpcheck
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: HttpCheck is the Schema for the httpchecks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HttpCheckSpec defines the desired state of HttpCheck
            properties:
              check_interval_in_seconds:
                type: integer
              name:
                type: string
              num_retries:
                type: integer
              response_status_code:
                type: integer
              uri:
                type: string
            type: object
          status:
            description: HttpCheckStatus defines the observed state of HttpCheck
            properties:
              observedStatus:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
